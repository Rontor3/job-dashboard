python-jobspy>=1.1.80
requests>=2.31
feedparser>=6.0
pytest>=8.0
sentence-transformers>=2.2
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pyyaml>=6.0

# Naukri source (optional — only needed for the vendored NopeRi Naukri
# fetcher; naukri_source.py import-guards these so nothing else requires
# them). curl_cffi replaces NopeRi's default httpcloak session backend
# (see src/job_dashboard/sources/naukri_session_shim.py); pycryptodome
# backs NopeRi's nkparam signing (src/utils/nkparam_generator.py).
curl_cffi>=0.7
pycryptodome>=3.20
selenium
beautifulsoup4
undetected-chromedriver
certifi

# Career agent (Phase 1: perception harness). Browser automation via the
# real Chromium build; nothing here evades bot-detection.
playwright>=1.44

# Career agent (Phase 2: remote-solve live-view server).
aiohttp>=3.9

# Career agent (LangGraph orchestration: checkpointed graph + interrupt-based human gate).
langgraph>=1.2
langgraph-checkpoint-sqlite>=3.0
mcp[server]>=1.0
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.1
